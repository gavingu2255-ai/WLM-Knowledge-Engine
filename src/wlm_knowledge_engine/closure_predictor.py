"""
closure_predictor.py — Predict future knowledge states (closures).

MVP behavior:
    - No real closure prediction.
    - Returns empty list.
"""

def predict_closures(graph):
    """
    Predict closures in the knowledge graph.

    Returns
    -------
    list
        List of closures (MVP: empty).
    """
    return []
